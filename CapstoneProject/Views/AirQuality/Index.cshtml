
@{
    ViewBag.Title = "Index";
}


<div id='map' style='height:500px;' />
<script src="https://maps.googleapis.com/maps/api/js"></script>

<script>
  
    var  map  =  new  google.maps.Map(document.getElementById('map'),  {
                  center:  new  google.maps.LatLng(51.505,  -0.09),
                  mapTypeId:  google.maps.MapTypeId.ROADMAP,
                  zoom:  8
    });

    var marker = new google.maps.Marker({
        position: new google.maps.LatLng(51.505, -0.09),
        label: "A",
        map: map

    }, function (response, status) {
            if (status == google.maps.DirectionsStatus.OK) {
                directionsDisplay.setDirections(response);
            } else {
                window.alert('Directions request failed due to' + status);
            }
    });
}

    function calculateAndDisplayRoute(directsService, directionsDisplay, pointA, pointB) {
        directsService.route({
            origin: pointA,
            destination: pointB,
            avoidHighways: false,
            travelMode: google.maps.TravelMode.DRIVING
        })
    }

    var directService = new google.maps.DirectionsService;
    var directDisplay = new google.maps.DirectionsRenderer({
        map: map
    });

    var pointA = new google.maps.LatLng(51.505, -0.09);
    var PointB =  new google.maps.LatLng(52.055, -0.09);
    var  waqiMapOverlay  =  new  google.maps.ImageMapType({
                  getTileUrl:  function(coord,  zoom)  {
                            return  'https://tiles.aqicn.org/tiles/usepa-aqi/'  +  zoom  +  "/"  +  coord.x  +  "/"  +  coord.y  +  ".png?token=5422120c5e5008629c1d2e647e006b1a9d124514";
                  },
                  name:  "Air  Quality",
        });

      map.overlayMapTypes.insertAt(0,waqiMapOverlay);
</script>

